# Automatic environment switching with direnv
# https://direnv.net/

# The base url for this website. Default is drupal's quick-start, which runs on port 8888 of localhost.
export DRUSH_OPTIONS_URI=http://localhost:8888

# Add the current composer bin directory to $PATH
export PATH=$(pwd)/vendor/bin:$PATH
