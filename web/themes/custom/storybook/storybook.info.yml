name: Storybook
type: theme
description: 'Base theme for using storybook components.'
package: Core
version: VERSION
core: 8.x

regions:
  content: 'Content'

component-libraries:
  storybook:
    paths:
      - ../../../../storybook/twig

libraries:
  - storybook/storybook
